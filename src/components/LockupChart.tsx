import { AccountLockup } from "near-lockup-helper"

export interface Props {
    lockupInfo: AccountLockup,
}

function LockupChart(props: Props) {
    return <div>TODO: chart</div>
}

export default LockupChart